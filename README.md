# Crackle Pop

A simple Ruby implementation of the CracklePop exercise. It prints `Crackle` for multiples of 3, `Pop` for multiples of 5, and `CracklePop` for multiples of both.

## Run

```sh
ruby -r ./crackle_pop -e 'crackle_pop'
```

## Test

```sh
ruby tests.rb
```
